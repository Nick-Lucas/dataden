{
  "name": "mydata",
  "version": "1.0.0",
  "description": "Personal and Extensible Data Warehouse",
  "author": "Nick Lucas",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "yarn eslint '*/**/*.{js,ts,jsx,tsx}'",
    "lint:fix": "yarn run lint --fix",
    "dev": "wsrun --parallel --exclude-missing watch"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@mydata/build": "1.0.0"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
